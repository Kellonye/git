Question 1:
Define a function that takes one string, text, as input and returns the text with all of the vowels removed.
For example: function_name("Hey You!") should return "Hy Y!". Don’t count Y as a vowel.
Make sure to remove lowercase and uppercase vowels.


Answer:

function remVowel(str) {
    let al = ['a', 'e', 'i', 'o', 'u',
          'A', 'E', 'I', 'O', 'U']

    let result = ""
    for(let i = 0; i < str.length; i++)
    {
         
        if (!al.includes(str[i]))
        {
            result += str[i];
        }
}
return result;
}

let str = "Iheme Morgan is a political jobber and tout";
document.write(remVowel(str));

// the string: 'Iheme Morgan is a political jobber and tout' will return: 'hm mrgn s pltcl jbbr nd tt'
